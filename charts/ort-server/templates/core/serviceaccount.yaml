apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ortserver.fullname" . }}-core-sa
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ortserver.labels" . | nindent 4 }}
automountServiceAccountToken: false
